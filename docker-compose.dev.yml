# Development override — uses Django dev server with auto-reload
version: '3.9'

services:
  web:
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    environment:
      - DEBUG=True
    ports:
      - "8000:8000"
